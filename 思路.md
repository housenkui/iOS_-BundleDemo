# 按「多 Bundle」方式构建自有 App 的思路

支付宝等大型 App 在 IPA 里呈现大量 Bundle，本质是：**把功能、资源、甚至代码库拆成多个「可独立分发与加载的单元」**。下面是一套可落地的思路，便于你从拆解观察过渡到自建工程。

---

## 1. 先弄清「你看到的 Bundle」可能是什么

在 IPA / `.app` 里常见的 Bundle 类型包括：

| 类型 | 典型路径 / 后缀 | 作用简述 |
|------|------------------|----------|
| **主程序包** | `YourApp.app` | 可执行文件 + 主资源 |
| **Framework** | `*.framework` | 动态库 + 头/模块 + 资源（可内含 `.bundle`） |
| **Resource Bundle** | `*.bundle` | 多为图片、JSON、html、文案等纯资源 |
| **Plug-in / Extension** | `PlugIns/*.appex` | 小组件、推送扩展、Share Extension 等 |
| **Assets / 本地化** | `*.car`、`*.lproj` | 编译后的资源目录 |

「全是 Bundle」往往 = **多 Framework + 多资源 Bundle + 若干 Extension**，而不是只有一个扁平的 `.app`。

---

## 2. 为什么要拆成很多 Bundle（设计动机）

- **团队与边界**：不同业务线对应不同模块，独立编译、独立发版节奏（内部流水线）。
- **资源隔离**：图片/音频/皮肤按模块打包，避免主工程资源爆炸、命名冲突。
- **按需加载**：部分资源或代码路径可按业务懒加载（配合路由、插件化思路）。
- **复用与合规**：支付、账号等能力封装在独立 Framework，便于审计与复用。
- **体积与缓存策略**：资源 Bundle 可细分，便于增量更新或 AB（若配合自有分发能力）。

注意：纯 App Store 分发下，**动态下发可执行代码**受限；多数情况下仍是 **预置在包内的 Framework + 资源 Bundle**，「插件化」多在壳 + 内置模块的意义上讨论。

---

## 3. 目标架构怎么定（推荐分层）

自下而上可分为：

1. **基础层**：网络、日志、路由、存储、账号会话等 → 一个或多个 **Framework**。
2. **业务模块层**：每个大业务一个 **Framework**（或静态库 + 资源 Bundle）。
3. **壳（Host App）**：极薄，负责启动、Tab、全局容器、注册路由与依赖注入。
4. **扩展**：需要的 **App Extension** 单独 Target，天然是独立 **appex Bundle**。

资源策略：

- 模块内 **Asset Catalog** 随模块 Target 走；或编译成 **`*.bundle`** 仅装资源，由模块代码按 `Bundle(for:)` / `Bundle(path:)` 加载。
- 跨模块共享资源放在 **SharedKit** 或 **DesignSystem** Framework 中。

---

## 4. Xcode 工程层面怎么落地

### 4.1 多 Target

- 主 App Target：`Application`
- 各业务：`Framework`（Dynamic Framework 更贴近「一模块一 Bundle」；Static Library + Bundle 也可）
- 小组件等：`App Extension` Target

### 4.2 依赖管理

- **Swift Package Manager**：按 Package / Module 拆分，适合模块化与版本约束。
- **CocoaPods**：多 Pod / subspec，适合存量项目。
- **本地多 Project + Workspace**：大厂常见，配合 CI 按模块编译。

### 4.3 资源单独打包为 `.bundle`

- 新建 **Bundle** 类型 Target（macOS/iOS 资源包），或将资源放入 Framework 的 **Resources** 构建阶段。
- 运行时加载注意：`Bundle.main` 不一定是模块 Bundle，需用 **类型所在模块的 Bundle** 查资源。

### 4.4 路由与模块注册

- 壳在启动时注册：`模块 ID → 入口类 / 工厂`，避免业务模块之间直接强引用链（降低编译耦合）。
- URL / 路由表可与后端配置结合，但页面实现仍建议 **内置在包内**。

---

## 5. 与支付宝式的差异与边界

- **能学的**：多模块、多 Framework、资源分包、壳工程、CI 分模块构建。
- **不必复制的**：具体插件协议、热修复方案与内部基建；受苹果政策与体积审查约束，个人/中小团队应优先 **正规模块化 + 清晰 Bundle 边界**，而不是追求「动态装插件」的同款。

---

## 6. 建议的实施顺序（本仓库后续可对照执行）

1. 建 Workspace：**壳 App + 1 个 Core Framework + 1 个 Demo 业务 Framework**。
2. 在 Demo 业务中放入 **独立 Asset / .bundle**，验证资源加载路径。
3. 引入 **路由或协议式导航**，让壳只依赖接口，业务依赖实现。
4. 再按业务复制 Target / SPM 模块，规范命名与目录结构。
5. 配置 **`.gitignore`**（已具备）与 CI：**按 Scheme 编译单模块或全量**。

---

## 7. 风险与注意点

- **动态 Framework 数量**：过多可能影响冷启动与链接时间，需 profiling。
- **符号与体积**：重复依赖同一第三方库时，需统一依赖版本或用 **静态链接策略** 合并。
- **访问权限**：Extension 与主 App 的 App Group、Keychain 共享需单独设计。

---

## 8. 小结

「支付宝式」包体 = **壳 + 多个动态库 Framework + 多个资源 Bundle + 扩展**。你要做的是：**先把模块边界和壳工程定清楚，再用 Xcode 多 Target / SPM 把代码和资源拆进不同 Bundle，最后配合路由与构建流水线**。本文件可作为后续在本目录脚手架实现的蓝图；若你确定只用 SPM 或只用 CocoaPods，下一步可把依赖策略写死并生成最小可运行 Demo。
